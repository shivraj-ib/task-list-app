import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const EditForm = () => {
  return (
      <Text>Edit form Component</Text>
  );
}

const styles = StyleSheet.create({});

export default EditForm;