import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const CreateForm = () => {
  return (
      <Text>Create Form Component</Text>
  );
}

const styles = StyleSheet.create({});

export default CreateForm;